name: SonarQube Scan

on:
  push:
    branches: [ main, master ]
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  sonarqube:
    name: SonarQube Scan
    runs-on: [self-hosted, internal-runner]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      - name: Install & Run Sonar Scanner
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          sudo apt-get update && sudo apt-get install -y unzip
          curl -sSLo /tmp/sonar-scanner.zip https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-5.0.1.3006-linux.zip
          sudo unzip -o /tmp/sonar-scanner.zip -d /opt
          sudo ln -sf /opt/sonar-scanner-5.0.1.3006-linux/bin/sonar-scanner /usr/local/bin/sonar-scanner
          sonar-scanner \
            -Dsonar.projectKey=cntm-labs_isotime \
            -Dsonar.projectName=isotime \
            -Dsonar.sources=. \
            -Dsonar.host.url=http://sonarqube.devops.svc.cluster.local:9000 \
            -Dsonar.exclusions="**/*.java, **/node_modules/**, **/dist/**, **/build/**, **/.git/**" \
            -Dsonar.login=${{ secrets.SONAR_TOKEN }}
